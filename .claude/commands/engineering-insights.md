---
description: Manually log this session's non-obvious lessons to the touched module's LEARNINGS.md
---

Apply the `engineering-insights` skill now: review this session's work and
append any actionable-cold lessons to the `LEARNINGS.md` of whichever
module(s) were touched (`server/`, `client/`, `reviewer-core/`, `e2e/` —
`repo-intel` shares `server/LEARNINGS.md`). Skip this if nothing non-obvious
came up.
