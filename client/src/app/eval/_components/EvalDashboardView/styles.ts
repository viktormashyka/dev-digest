import type { CSSProperties } from "react";

/** Co-located styles for the /eval dashboard. */
export const s = {
  page: { maxWidth: 1200, margin: "0 auto", padding: "28px 32px 60px", display: "flex", flexDirection: "column", gap: 28 } satisfies CSSProperties,
  header: { display: "flex", alignItems: "flex-start", gap: 16 } satisfies CSSProperties,
  headerText: { flex: 1 } satisfies CSSProperties,
  h1: { fontSize: 22, fontWeight: 700 } satisfies CSSProperties,
  h2: { fontSize: 16, fontWeight: 700 } satisfies CSSProperties,
  subtitle: { fontSize: 14, color: "var(--text-secondary)", marginTop: 4 } satisfies CSSProperties,
  note: { fontSize: 13, color: "var(--text-muted)" } satisfies CSSProperties,
  section: { display: "flex", flexDirection: "column", gap: 12 } satisfies CSSProperties,
  agentList: { display: "flex", flexDirection: "column", gap: 8 } satisfies CSSProperties,
  agentRow: {
    display: "flex",
    alignItems: "center",
    gap: 16,
    padding: "14px 16px",
    borderRadius: 9,
    border: "1px solid var(--border)",
    background: "var(--bg-elevated)",
    width: "100%",
  } satisfies CSSProperties,
  agentMain: { display: "flex", flexDirection: "column", gap: 2, minWidth: 180 } satisfies CSSProperties,
  agentName: { fontSize: 14, fontWeight: 600, color: "var(--text-primary)", background: "none", border: "none", padding: 0, cursor: "pointer", textAlign: "left" } satisfies CSSProperties,
  agentMeta: { fontSize: 12.5, color: "var(--text-muted)" } satisfies CSSProperties,
  agentMetrics: { display: "flex", gap: 20, flex: 1 } satisfies CSSProperties,
  metricCol: { display: "flex", flexDirection: "column", gap: 2 } satisfies CSSProperties,
  metricLabel: { fontSize: 11, fontWeight: 700, letterSpacing: "0.04em", textTransform: "uppercase", color: "var(--text-muted)" } satisfies CSSProperties,
  metricVal: { fontSize: 15, fontWeight: 600, color: "var(--text-primary)" } satisfies CSSProperties,
  visuallyHidden: {
    position: "absolute",
    width: 1,
    height: 1,
    overflow: "hidden",
    clip: "rect(0 0 0 0)",
    whiteSpace: "nowrap",
  } satisfies CSSProperties,
  table: { width: "100%", borderCollapse: "collapse", fontSize: 13 } satisfies CSSProperties,
  th: {
    textAlign: "left",
    padding: "8px 10px",
    fontSize: 11.5,
    fontWeight: 700,
    letterSpacing: "0.04em",
    textTransform: "uppercase",
    color: "var(--text-muted)",
    borderBottom: "1px solid var(--border)",
  } satisfies CSSProperties,
  td: { padding: "8px 10px", borderBottom: "1px solid var(--border)", color: "var(--text-secondary)" } satisfies CSSProperties,
  outcome: (color: string): CSSProperties => ({
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
    fontSize: 12.5,
    fontWeight: 600,
    color,
  }),
  modalBody: { padding: "20px 24px", display: "flex", flexDirection: "column", gap: 14 } satisfies CSSProperties,
  footer: { display: "flex", justifyContent: "flex-end", gap: 10 } satisfies CSSProperties,
  resultRow: { display: "flex", alignItems: "center", gap: 10, fontSize: 13.5 } satisfies CSSProperties,
} as const;
