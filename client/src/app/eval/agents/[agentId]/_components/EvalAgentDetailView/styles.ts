import type { CSSProperties } from "react";

/** Co-located styles for the /eval/agents/:id detail view. */
export const s = {
  page: { maxWidth: 1000, margin: "0 auto", padding: "28px 32px 60px", display: "flex", flexDirection: "column", gap: 24 } satisfies CSSProperties,
  header: { display: "flex", alignItems: "center", gap: 12 } satisfies CSSProperties,
  h1: { fontSize: 22, fontWeight: 700 } satisfies CSSProperties,
  h2: { fontSize: 16, fontWeight: 700 } satisfies CSSProperties,
  headSpacer: { marginLeft: "auto" } satisfies CSSProperties,
  callout: {
    fontSize: 13.5,
    padding: "10px 14px",
    borderRadius: 8,
    border: "1px solid var(--border-strong)",
    background: "var(--bg-elevated)",
    color: "var(--text-secondary)",
  } satisfies CSSProperties,
  tiles: { display: "flex", gap: 12, flexWrap: "wrap" } satisfies CSSProperties,
  tile: {
    flex: 1,
    minWidth: 140,
    background: "var(--bg-elevated)",
    border: "1px solid var(--border)",
    borderRadius: 9,
    padding: 18,
  } satisfies CSSProperties,
  tileLabel: { fontSize: 12, fontWeight: 600, color: "var(--text-muted)", letterSpacing: "0.03em" } satisfies CSSProperties,
  tileValue: { fontSize: 28, fontWeight: 700, marginTop: 10 } satisfies CSSProperties,
  tileDelta: (color: string): CSSProperties => ({ fontSize: 12.5, fontWeight: 600, color, marginTop: 4 }),
  section: { display: "flex", flexDirection: "column", gap: 12 } satisfies CSSProperties,
  visuallyHidden: {
    position: "absolute",
    width: 1,
    height: 1,
    overflow: "hidden",
    clip: "rect(0 0 0 0)",
    whiteSpace: "nowrap",
  } satisfies CSSProperties,
  table: { width: "100%", borderCollapse: "collapse", fontSize: 13 } satisfies CSSProperties,
  th: {
    textAlign: "left",
    padding: "8px 10px",
    fontSize: 11.5,
    fontWeight: 700,
    letterSpacing: "0.04em",
    textTransform: "uppercase",
    color: "var(--text-muted)",
    borderBottom: "1px solid var(--border)",
  } satisfies CSSProperties,
  td: { padding: "8px 10px", borderBottom: "1px solid var(--border)", color: "var(--text-secondary)" } satisfies CSSProperties,
  outcome: (color: string): CSSProperties => ({
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
    fontSize: 12.5,
    fontWeight: 600,
    color,
  }),
  footer: { display: "flex", justifyContent: "flex-end", gap: 10 } satisfies CSSProperties,
  modalBody: { padding: "20px 24px", display: "flex", flexDirection: "column", gap: 16 } satisfies CSSProperties,
  diffLine: (kind: "added" | "removed" | "context"): CSSProperties => ({
    margin: 0,
    color: kind === "added" ? "var(--ok, #3fb950)" : kind === "removed" ? "var(--crit)" : "var(--text-secondary)",
    background: kind === "added" ? "var(--ok-bg, #052e1c)" : kind === "removed" ? "var(--crit-bg)" : "transparent",
  }),
  pre: {
    margin: 0,
    padding: 12,
    borderRadius: 7,
    border: "1px solid var(--border-strong)",
    background: "var(--bg-elevated)",
    fontSize: 12.5,
    lineHeight: 1.5,
    whiteSpace: "pre-wrap",
    wordBreak: "break-word",
    maxHeight: 320,
    overflow: "auto",
  } satisfies CSSProperties,
} as const;
