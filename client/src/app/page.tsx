import { HomeView } from "./_components/HomeView";

/* Route: / — thin route entry. The view, its styles and redirect logic are
   colocated under _components/HomeView. */
export default function HomePage() {
  return <HomeView />;
}
